{
  "version": "0.3.1",
  "min_version": "0.1.0",
  "changelog": "Status LED fixes: it no longer fails to start at boot when udev sets the PWM permissions late, and it no longer flickers between states at boot because of the clock (the Pi has no RTC). OTA updates now flush writes to the SD card, so a power cut right after an update can no longer leave empty files or a corrupted git repository."
}
