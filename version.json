{
  "version": "0.1.7",
  "min_version": "0.1.0",
  "changelog": "Fix: plymouth quit --retain-splash silently failed (ran as unprivileged user, no access to the daemon control socket), leaving a grey screen instead of the frozen boot animation before the app takes over"
}
